import React from 'react';
import { Navigate } from 'react-router-dom';

const AcademicSettings = () => {
    return <Navigate to="/profile/edit/basic" replace />;
};

export default AcademicSettings;
